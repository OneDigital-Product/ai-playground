# Example local-only overrides for the host gateway.
# Copy to .env.local to avoid committing secrets/defaults.
# Next.js will load .env, then .env.local which overrides.

# Local dev origins. Keep them http (no trailing slash) and explicit ports.
WEB_ORIGIN=http://localhost:3001
ADMIN_ORIGIN=http://localhost:3002
# If you run reporting locally, point here; otherwise leave as a stable remote.
REPORTING_ORIGIN=http://localhost:4000


# Convex (planned integration):
# For local dev, set to your Convex deployment URL. In CI (Vercel),
# this is injected by the Convex deploy command.
NEXT_PUBLIC_CONVEX_URL=https://your-convex-url.convex.cloud
