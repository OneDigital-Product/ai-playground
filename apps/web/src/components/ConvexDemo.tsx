export default function ConvexDemo() {
  return (
    <div className="p-6 max-w-2xl mx-auto">
      <h1 className="text-2xl font-bold mb-4">Convex Demo</h1>
      <p className="text-muted-foreground">
        The interactive Convex demo is not configured in this workspace. Add a
        Convex backend and set NEXT_PUBLIC_CONVEX_URL to enable it.
      </p>
    </div>
  );
}
